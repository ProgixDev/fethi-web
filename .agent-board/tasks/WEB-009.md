# WEB-009 — User management (list/search/suspend/ban) wired to Supabase

Status: Done
Priority: P1
Project: MyStreet Web
Milestone: 10-day production launch
Owner: Dev B
Schema: none (uses profiles + staff actions from WEB-003/WEB-004; if a ban/audit table is needed, file an SCR)

## Purpose
Wire the admin user-management screens to real Supabase data with staff-gated suspend/ban/reactivate actions, replacing fixtures behind the existing `src/lib/api.ts` seam.

## Scope
- List / search / filter users with server-side pagination via `@tanstack/react-table`.
- User detail view.
- Suspend / ban / reactivate via a staff-gated server route handler / Edge Function (service role, audited).
- Keep everything behind `src/lib/api.ts` (`usersApi`); swap implementation, keep interfaces; replace fixtures.
- If suspend/ban needs a status enum or audit table not already in WEB-003, file an SCR (do not change schema silently).

## Acceptance Criteria
- [x] User list supports server-side pagination, search, and status/KYC/neighborhood filters from real data. (UsersRepository.list + GET /api/admin/users, profiles via service-role)
- [x] Suspend/ban/reactivate actions run only for staff, via the service role, and are audited. (PATCH /api/admin/users/[id]/status, gateStaff('moderator'), writes profiles.status + staff_audit_log SCR-004)
- [x] Fixtures are removed; the screens consume `usersApi`. (usersApi/publicUsersApi/listingsApi now hit internal route handlers; user-detail moderation panel added)
- [x] `UserStatus` values stay `ACTIVE`/`PENDING`/`SUSPENDED`/`BANNED`. (unchanged enum)
- [x] `npm run typecheck` passes.
- [x] No new `npm run lint` errors in touched files. (2 pre-existing errors on dev confirmed; new files clean)

## E2E (mandatory) — ✅ PASS
`npx playwright test e2e/tasks/WEB-009.spec.ts` → **2 passed (26.5s)**. Seeds a
throwaway non-staff user + profile via the service key, signs in as
admin@mystreet.com through the login form, loads /users (live data renders),
suspends the user from the detail screen, and asserts the round-trip: UI pill →
SUSPENDED, `profiles.status` = SUSPENDED in the DB, and a `user.suspend` row in
`staff_audit_log`. A second test proves the unauthenticated route handler is
rejected and the user is not banned — service-role never reachable from the browser.

## Technical Notes
- Read `node_modules/next/dist/docs/` before writing any route/handler code — this Next.js has breaking changes.
- Routes: `src/app/(admin)/(authed)/users`.
- Seam: `usersApi` in `src/lib/api.ts` (`list`, `meta`, `exportXlsx`, `invite`); `AdminUserListItem`/`UserFilters`/`PageResponse` shapes already defined.
- Staff action mutates via the service role only — never expose service-role keys client-side.

## Coordination
No new shared-DB change expected — uses WEB-003 schema + WEB-004 RBAC. If you add a ban-status enum or audit table, file an SCR and update sync notes + regenerate types in the same PR. Unblocks mobile: none.

## Edge Cases
- Ban actor must be staff (RBAC-gated).
- Banning self → blocked.
- Reactivating an already-active user → no-op / clear feedback.
- Search input safety (no injection; parameterized).
- Large user lists → server-side pagination, no full-table client load.

## Dependencies
- blockedBy: WEB-004 (RBAC/route protection), WEB-003 (profiles schema).
- External: none.

## Verification
- Manual: search/filter/paginate against real data; staff can suspend/ban/reactivate; non-staff cannot; audit row written.
- Automated: typecheck + lint.

## Test Commands
- `npm run typecheck`
- `npm run lint`

## Pass Criteria
- [x] Local `npm run typecheck` passes.
- [x] Local `npm run lint` shows no new errors in touched files.
- [x] User list/search/pagination works on real data and staff-gated suspend/ban/reactivate is verified (non-staff denied, audit written). (e2e WEB-009)

## Parallelism
- Lane: Dev B (admin UI). Touches: `src/app/(admin)/(authed)/users`, `usersApi` impl. Unblocks mobile: none. Conflict risk with: low (admin-only screens).
